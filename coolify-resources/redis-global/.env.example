# redis-global env template (Phase 1.5). Real value in .env (gitignored, beside this).
# Also mirrored in /root/.coolify-secrets-backup.txt (redis_global_password) + Bitwarden.
REDIS_PASSWORD=REPLACE_ME
