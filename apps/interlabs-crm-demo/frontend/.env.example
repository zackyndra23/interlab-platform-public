# Frontend environment (Next.js reads variables prefixed with NEXT_PUBLIC_
# at build time; everything else is server-only).
NEXT_PUBLIC_APP_NAME=Interlabs CRM
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000/api/ws
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
